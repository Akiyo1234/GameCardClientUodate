# แผนการดำเนินงานการแก้ไขโครงงาน: ระบบเกมการ์ดเสริมสร้างแนวคิดระบบสารสนเทศ

เอกสารนี้รวบรวมแผนงานและขั้นตอนการปฏิบัติงาน (skill/task list) สำหรับการนำไปใช้งานจริงในโปรเจค โดยอ้างอิงจากแผนงาน 4 สัปดาห์

## 🎯 วัตถุประสงค์หลักของการแก้ไข
1. ยกระดับสถาปัตยกรรมระบบให้เป็น **Cloud-based Architecture** (ใช้ Supabase เป็น Single Source of Truth)
2. เพิ่มระบบ **Board State Persistence** (รองรับ Reconnect)
3. เปลี่ยนการจัดเก็บคำถามจาก Local Storage เป็น **Online Database** 100%
4. พัฒนาโหมด **Interactive Tutorial** และปรับปรุง UI เว็บไซต์ผู้ดูแลระบบ (Admin Web)

---

## 📅 แผนการดำเนินงานรายสัปดาห์ (Task Checklist)

### สัปดาห์ที่ 1: ระบบคำถามออนไลน์ 100% และเว็บผู้ดูแลระบบ
- [ ] **วันที่ 1-2: แก้ไขโค้ด Unity (QuizManager.cs)**
  - [ ] เปลี่ยนไปเรียกใช้ `get_active_questions()` ผ่าน Supabase SDK แทนการอ่านไฟล์ JSON เดิม
  - [ ] ตรวจสอบว่าเกมสามารถโหลดคำถามจาก Database ได้สำเร็จ
- [ ] **วันที่ 3-4: พัฒนาหน้า Admin Web (Create Form)**
  - [ ] สร้างแบบฟอร์มเพิ่มคำถามทีละข้อ (โจทย์, ตัวเลือก ก-ง, เฉลย, หมวดหมู่, ระดับความยาก)
  - [ ] เพิ่มปุ่ม "บันทึก" เพื่อ INSERT ลงตาราง `quiz_questions`
- [ ] **วันที่ 5-6: พัฒนาหน้า Admin Web (Data Table)**
  - [ ] สร้างตารางแสดงรายการคำถามทั้งหมด พร้อมระบบ Filter ตามหมวดหมู่และระดับ
  - [ ] รองรับการค้นหา (Keyword) และสร้างปุ่ม Edit / Delete ในแต่ละแถว
- [ ] **วันที่ 7: ทดสอบ End-to-End**
  - [ ] ทดสอบเพิ่มคำถามจากเว็บ -> เปิดเกม -> เจอคำถามใหม่ทันทีโดยไม่ต้องอัปเดตเกม

### สัปดาห์ที่ 2: ระบบบันทึกสถานะกระดาน (ส่วน Serialize)
- [ ] **วันที่ 1: จัดการ Database**
  - [ ] รัน Migration สร้างตาราง `match_sessions` บน Supabase (id, room_code, board_state, status, created_at, updated_at)
  - [ ] กำหนดสิทธิ์ (RLS) ให้ผู้เล่นอ่านได้เฉพาะ active session, เซิร์ฟเวอร์มีสิทธิ์เขียน/แก้ไข/ลบ
- [ ] **วันที่ 2-3: พัฒนาฝั่ง Unity (Serialize)**
  - [ ] สร้าง Script `BoardStateSerializer.cs`
  - [ ] เขียนฟังก์ชัน `Serialize()` กวาดข้อมูลกระดาน (ทรัพยากร, การ์ด, สถานะผู้เล่น, ฯลฯ) แพ็คเป็น JSON ก้อนเดียว
- [ ] **วันที่ 4-5: เชื่อมต่อ Database (INSERT & UPDATE)**
  - [ ] เขียนลอจิกตอนเริ่มเกม (Setup Phase): สร้างแถวใหม่ใน `match_sessions` โดยผูกกับ `room_code`
  - [ ] เขียนลอจิกตอนจบเทิร์น: เรียก `Serialize()` แล้วส่งคำสั่ง UPDATE ไปยัง `board_state` ของ session นั้น
- [ ] **วันที่ 6-7: ทดสอบ Serialize**
  - [ ] ลองเล่นเกม 5 เทิร์น และตรวจสอบข้อมูล JSON ใน Supabase Dashboard ว่าตรงกับสถานะกระดานจริงหรือไม่

### สัปดาห์ที่ 3: ระบบ Reconnect (ส่วน Deserialize + Disconnect)
- [ ] **วันที่ 1-2: พัฒนาฝั่ง Unity (Deserialize)**
  - [ ] ใน `BoardStateSerializer.cs` เขียนฟังก์ชัน `Deserialize()` 
  - [ ] อ่าน JSON จาก Database และสร้างกระดาน (วางการ์ด/เหรียญ/สถานะผู้เล่น) กลับมาให้ถูกต้อง
- [ ] **วันที่ 3: ลอจิกการกลับเข้าเกม (Login)**
  - [ ] เพิ่มลอจิกหลัง Login เสร็จ: Query ตาราง `match_sessions` หา Active Session ที่ผู้เล่นค้างอยู่
  - [ ] หากพบ ให้แสดงปุ่ม "กลับเข้าเกม" บนหน้า Lobby เพื่อดึง JSON มา `Deserialize()` และเชื่อมต่อเข้า Photon Room เดิม
- [ ] **วันที่ 4-5: จัดการการ Disconnect (DisconnectManager.cs)**
  - [ ] ตรวจจับ Disconnect และไม่หยุดเกมบล็อกผู้เล่นอื่น
  - [ ] เขียนลอจิก "เล่นแทนอัตโนมัติ" เมื่อหมดเวลาเทิร์น (Turn Timer) ของคนที่หลุด
  - [ ] รองรับ Quick Rejoin (Photon) และกำหนดเวลา Forfeit Logic (เช่น หายนานเกิน 3 นาทีปรับแพ้หัก MMR และอัปเดตสถานะ session เป็น abandoned)
- [ ] **วันที่ 6-7: ทดสอบ Reconnect**
  - [ ] ปิด Wi-Fi ระหว่างเล่น -> รอสักพัก -> เปิด Wi-Fi และกดกลับเข้าเกม -> ระบบต้องฟื้นฟูเกมที่จุดล่าสุดได้สำเร็จ

### สัปดาห์ที่ 4: โหมดสอนเล่น Interactive & ทดสอบรวม
- [ ] **วันที่ 1-2: โครงสร้าง Tutorial**
  - [ ] สร้าง `TutorialScene` ใหม่ (เล่นแบบ Offline ไม่ต้องเชื่อมต่อ Photon)
  - [ ] สร้าง `TutorialManager.cs` ใช้ State Machine ควบคุมลำดับขั้นตอนสอนเล่น
- [ ] **วันที่ 3-4: UI Masking & Steps**
  - [ ] ใช้ Canvas UI วาง Panel สีดำ (Alpha 75%) ทับทั้งจอ และเจาะรูเฉพาะปุ่มเป้าหมาย (ใช้ RectMask2D หรือ Custom Shader)
  - [ ] ล็อกปุ่มอื่นๆ ทั้งหมด (`button.interactable = false`)
  - [ ] จัดทำ Flow 10 Steps: สอนตอบคำถาม -> หยิบเหรียญ -> ยกเลิก -> หยิบเหรียญซ้ำ -> จบเทิร์น -> ซื้อการ์ด -> จองการ์ด -> รวบรวมอีกรอบ -> ซื้อการ์ดที่จอง -> อธิบายวิธีชนะ
- [ ] **วันที่ 5: ทดสอบ Tutorial**
  - [ ] ให้ผู้เล่นใหม่ทดลองเล่นว่าเข้าใจกลไกเกมผ่านโหมดนี้หรือไม่
- [ ] **วันที่ 6-7: Integration Test**
  - [ ] ทดสอบระบบทั้งหมดรวมกัน (Integration Test) ตรวจสอบบั๊กและความเสถียรของระบบ (QA Testing) ตาม Test Plan

---

## 🧪 สรุปกรณีทดสอบ (Test Cases)
*อ้างอิงเพื่อนำไปประเมินผลระบบก่อนขึ้นใช้งานจริง*

1. **เพิ่มคำถามใหม่:** ใส่คำถามผ่านเว็บ Admin -> เปิดเกมใหม่ต้องเจอคำถามนั้น
2. **ลบคำถาม:** ลบจากเว็บ Admin -> คำถามนั้นต้องไม่ปรากฏในเกม
3. **Save Board State:** เล่น 5 เทิร์น ข้อมูล JSONB ใน DB ต้องสะท้อนการเดินจริง
4. **Reconnect สำเร็จ:** ปิดเน็ต 30 วิ -> เปิดใหม่ -> กลับเข้าเกมสำเร็จโดยคนอื่นไม่ต้องรอ
5. **Reconnect ไม่สำเร็จ (Forfeit):** ปิดเน็ตเกิน 3 นาที -> ระบบเล่นแทนชั่วคราวและปรับผู้เล่นหลุดแพ้ หัก MMR
6. **Tutorial Flow:** ผู้เล่นทำตามสเต็ป 1-10 จนจบได้
7. **Tutorial UI Lock:** ลองกดปุ่มอื่นๆ ที่ไม่ใช่เป้าหมายในแต่ละ Step ต้องไม่ตอบสนอง
